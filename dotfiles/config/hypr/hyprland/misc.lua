hl.config({
	-- General system and appearance settings
	misc = {
		disable_splash_rendering = true,
		disable_hyprland_logo = true,
		focus_on_activate = true,
		vrr = 0,
	},

	-- Mouse cursor configuration
	cursor = {
		zoom_factor = 1,
	},

	-- Keybinds and input event behavior
	binds = {
		scroll_event_delay = 0,
	},

	-- XWayland compatibility compatibility settings
	xwayland = {
		force_zero_scaling = true,
	},
})
